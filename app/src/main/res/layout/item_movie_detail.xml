<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ivMovieImageDetail"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        tools:src="@mipmap/ic_launcher"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:textAppearanceLarge"
        tools:text="Captain America"
        android:id="@+id/tvTitleDetail"
        android:layout_alignBottom="@+id/ivMovieImageDetail"
        android:layout_toRightOf="@+id/ivMovieImageDetail"
        android:layout_toEndOf="@+id/ivMovieImageDetail" />

    <TextView
        android:id="@+id/tvOverviewDetail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvTitle"
        android:layout_toEndOf="@+id/ivMovieImageDetail"
        android:layout_toRightOf="@id/ivMovieImageDetail"
        tools:text="Following the events of Age of..." />

</RelativeLayout>